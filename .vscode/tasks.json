
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tasks",
            "type": "shell",
            "command": "gcc -g *.c libft/*.c execution/*/*.c execution/*/*/*.c execution/*.c -lreadline",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}